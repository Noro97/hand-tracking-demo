# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Supabase env — the publishable key isn't secret (RLS is the real security
# boundary), but keep committing only the shape (.env.example), not values.
.env

# MediaPipe runtime assets — regenerated from node_modules by scripts/copy-mediapipe.mjs
# (runs via `postinstall`; see package.json)
/public/mediapipe/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
